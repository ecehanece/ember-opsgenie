export default function(defaultOptions) {
  defaultOptions.credits.href = 'http://www.opsgenie.com';
  defaultOptions.credits.text = 'Powered by OpsGenie';
  defaultOptions.credits.enabled = true;

  return defaultOptions;
}
